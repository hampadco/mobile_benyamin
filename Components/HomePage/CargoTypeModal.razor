@using MudBlazor

<link href="css/components/HomePage/CargoTypeModal.css" rel="stylesheet" />

<MudDialog @bind-IsVisible="IsOpen" Options="dialogOptions">
    <TitleContent>
        <MudText Typo="Typo.h4" Class="cargo-modal-title">نوع بار</MudText>
        <MudText Typo="Typo.subtitle1" Class="cargo-modal-subtitle">نوع بار خود را انتخاب کنید</MudText>
    </TitleContent>
    <DialogContent>
        <MudStack Spacing="4" Class="cargo-list">
            @foreach (var cargo in CargoTypes)
            {
                <MudPaper Elevation="0" Class="cargo-item" @onclick="() => SelectCargo(cargo.Name)">
                    <MudGrid Justify="Justify.SpaceBetween" AlignItems="AlignItems.Center">
                        <MudItem>
                            <MudStack Row="true" Spacing="3">
                                <MudPaper Elevation="0" Class="cargo-icon-container">
                                    <MudIcon Icon="@cargo.Icon" Color="Color.Primary" Size="Size.Large" />
                                </MudPaper>
                                <div class="cargo-info">
                                    <MudText Typo="Typo.subtitle2" Class="cargo-name">@cargo.Name</MudText>
                                    <MudText Typo="Typo.body2" Class="cargo-description">@cargo.Description</MudText>
                                </div>
                            </MudStack>
                        </MudItem>
                        <MudItem>
                            <MudRadioGroup T="string" @bind-SelectedOption="selectedCargo">
                                <MudRadio Value="cargo.Name" Color="Color.Primary" />
                            </MudRadioGroup>
                        </MudItem>
                    </MudGrid>
                </MudPaper>
            }
        </MudStack>
    </DialogContent>
    <DialogActions>
        <div class="cargo-actions">
            <button @onclick="OnClose" class="cargo-cancel-btn">لغو</button>
            <button @onclick="OnConfirm" class="cargo-confirm-btn">تایید</button>
        </div>
    </DialogActions>
</MudDialog>

@code {
    [Parameter]
    public bool IsOpen { get; set; }

    [Parameter]
    public EventCallback<bool> IsOpenChanged { get; set; }

    [Parameter]
    public EventCallback<string> OnCargoTypeSelected { get; set; }

    private string selectedCargo;

    private DialogOptions dialogOptions = new()
    {
        FullWidth = true,
        CloseButton = false,
        //DisableBackdropClick = true,
        NoHeader = false,
        //ClassBackground = "bg-b50 dark:bg-n50"
    };

    private class CargoType
    {
        public string Id { get; set; }
        public string Name { get; set; }
        public string Description { get; set; }
        public string Icon { get; set; }
    }

    private List<CargoType> CargoTypes = new()
    {
        new() { Id = "general", Name = "بار عمومی", Description = "کالاهای معمولی و عمومی", Icon = "fas fa-box" },
        new() { Id = "perishable", Name = "بار فاسدشدنی", Description = "مواد غذایی و فاسدشدنی", Icon = "fas fa-snowflake" },
        new() { Id = "dangerous", Name = "بار خطرناک", Description = "مواد شیمیایی و خطرناک", Icon = "fas fa-exclamation-triangle" },
        new() { Id = "heavy", Name = "بار سنگین", Description = "تجهیزات و ماشین‌آلات سنگین", Icon = "fas fa-truck" },
        new() { Id = "fragile", Name = "بار شکستنی", Description = "کالاهای شکستنی و حساس", Icon = "fas fa-wine-glass" }
    };

    private void SelectCargo(string cargo)
    {
        selectedCargo = cargo;
    }

    private async Task OnClose()
    {
        await IsOpenChanged.InvokeAsync(false);
    }

    private async Task OnConfirm()
    {
        if (!string.IsNullOrEmpty(selectedCargo))
        {
            await OnCargoTypeSelected.InvokeAsync(selectedCargo);
        }
        await OnClose();
    }
} 